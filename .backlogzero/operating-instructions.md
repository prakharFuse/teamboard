"# Repository Overview\n\n- This is a fixture repository (see README.md) — a small Node CLI that exists so agent journeys have a real repo to clone/branch/PR against. It is not a product with real users.\n- Run it with `npm start -- --help`, `npm start -- status`, or `npm start -- tasks` (see package.json scripts).\n\n# Code Style\n\n- Plain ESM (`\"type\": \"module\"` in package.json) — no TypeScript, no bundler, no lint config, and no runtime dependencies.\n- Every source file imports only Node built-ins or sibling `src/*.js` modules via relative `./x.js` paths.\n\n# FIXTURE NOTE Comments\n\n- `src/api-client.js`, `src/logger.js`, and `src/store.js` each open with a `FIXTURE NOTE` block comment describing an intentional design gap (hardcoded/scattered config, a global-only settings store, a logging level concept with no `--quiet` flag).\n- These look like TODOs but are not oversights — each is the deliberate subject of a separate, specific ticket. Do not \"clean up\" or fix what a FIXTURE NOTE describes unless the current task explicitly targets that exact gap.\n\n# Known Documentation Drift\n\n- README.md's Configuration section claims environment variables are read in `src/config.ts` — that file does not exist, and no source file in the repo reads `process.env`. All configuration is either a hardcoded literal in `src/api-client.js` / `src/logger.js` or an in-memory default in `src/store.js`. Do not trust the README's config pointer; verify against the actual source instead.\n\n# Testing\n\n- Run `npm test`, which uses Node's built-in `node --test` runner — there is no Jest/Mocha/Vitest, and package.json declares zero dependencies or devDependencies.\n- Test files live under `test/` and are named `*.test.js`, importing `strict as assert` and `test` from `node:test`.\n\n# PR Requirements\n\n- IMPORTANT: Every PR must add a bullet under the `## Unreleased` heading in `RELEASE_NOTES.md` describing the change. A PR that doesn't touch `RELEASE_NOTES.md` will be rejected on review. Append your bullet under the existing `## Unreleased` heading — do not recreate it."